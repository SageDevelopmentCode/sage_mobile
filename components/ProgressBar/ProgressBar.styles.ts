import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderRadius: 20,
    overflow: "hidden",
    height: 30,
  },
  progress: {
    height: "100%",
    borderRadius: 20,
  },
});
